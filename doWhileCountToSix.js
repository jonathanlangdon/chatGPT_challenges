// Write a do-while loop that counts up from numVar to 6. Ex: For numVar = 3, output is: 3 4 5 6 (with each value output on a separate line)

let numVar = 3;

do {
  console.log(numVar);
  numVar += 1;
} while (numVar <= 6);
