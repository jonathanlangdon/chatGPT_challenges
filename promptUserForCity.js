// Prompt the user, asking "What city do you live in?", then display the result as: Fall in ____ is pleasant.

const city = prompt('What city do you live in?');
console.log(`Fall in ${city} is pleasant.`);
