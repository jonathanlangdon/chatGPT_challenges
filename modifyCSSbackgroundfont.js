//set the paragraph's inline style to use the 10px font size and remove the paragraph's background color property.

const helloElem = document.querySelector('#helloMessage');

helloElem.style.fontSize = '10px';
helloElem.removeProperty('background');
