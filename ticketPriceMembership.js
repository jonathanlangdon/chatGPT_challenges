// Complete the conditional operator so ticketPrice is assigned with 11 if membershipLevel is 7 or above, or with 19 otherwise

let membershipLevel = 8;
let ticketPrice = membershipLevel >= 7 ? 11 : 19;

console.log(ticketPrice); // 11
