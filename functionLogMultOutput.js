// Declare and define a function called displayMult that uses console.log() to display the result of multiplying two parameters.

const displayMult = (n1, n2) => console.log(n1 * n2);

displayMult(3, 4);
