// construct a simple while loop

let enteredNum = 40; // Code will be tested with values: 40, 4 and -5

/* Your solution goes here */
while (enteredNum > 1) {
  enteredNum = enteredNum / 4;
  console.log(enteredNum);
}
