//Output the elements at indices 1 and 4 in the array userNumbers separated by spaces.

let userNumbers = [1, 6, 41, 8, 24, 4]; // Tests may use different array values
console.log(userNumbers[1] + ' ' + userNumbers[4]);
